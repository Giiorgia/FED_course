const Car = {
    manufacturer: 'Toyota',
    model: 'C-HR',
    yearOfRelease: 2023,
    avgSpeed: 100,
}

console.log(Car);

// // coverDistance: function(distance) {
// //     let distance = 400,
// //     return distance/avgSpeed,
// // }

let distance = 400;
let timeFrame = distance / avgSpeed;
let breakTime = timeFrame / 4;
let totalTime = timeFrame ++ breakTime;
return totalTime;

const Clock = {
    hours: 
    minutes:
    seconds:
}