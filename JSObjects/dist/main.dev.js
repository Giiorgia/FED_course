"use strict";

var Car = {
  manufacturer: 'Toyota',
  model: 'C-HR',
  yearOfRelease: 2023,
  avgSpeed: 100
};
console.log(Car); // // coverDistance: function(distance) {
// //     let distance = 400,
// //     return distance/avgSpeed,
// // }
// calculateTime(distance) {
//     let travelTime = distance / avgSpeed; // Time in hours without breaks
//     let breaks = Math.floor(travelTime / 4); // One 1-hour break every 4 hours
//     let totalTime = travelTime + breaks; // Adding break time
//     return totalTime;
// }
// let distance = 500; // Example distance in km
// let timeNeeded = car.calculateTime(distance); // Calculating time to cover the distance
// console.log(`Time needed to cover ${distance} km: ${timeNeeded.toFixed(2)} hours`);